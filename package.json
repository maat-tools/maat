{
	"name": "maat",
	"version": "0.0.1",
	"private": true,
	"description": "Architectural governance tool for TypeScript — deterministic detection, attributed decisions, permanent ledger, opt-in enforcement.",
	"keywords": ["architecture", "governance", "coupling", "connascence", "typescript", "static-analysis", "ledger"],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/maatjs/maat"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"lint": "biome check .",
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"maat": "bun run packages/cli/src/index.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "latest",
		"@types/bun": "latest",
		"typescript": "^5.0.0"
	}
}